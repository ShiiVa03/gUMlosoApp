<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.gumloso">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />



    <application

        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
        tools:targetApi="m">
        <activity
            android:name=".consumer.MapsActivity"
            android:exported="false" />
        <activity
            android:name=".ShiftsActivity"
            android:exported="false" />
        <activity
            android:name=".consumer.ShowReservation"
            android:exported="false" />
        <activity
            android:name=".consumer.ScrollReservations"
            android:exported="false" />
        <activity
            android:name=".consumer.CriarReserva"
            android:exported="false" />
        <activity
            android:name=".manager.EditManagerProfile"
            android:exported="false" />
        <activity
            android:name=".manager.EditRestaurant"
            android:exported="false" />
        <activity
            android:name=".manager.GestaoReservas"
            android:exported="false" />
        <activity
            android:name=".consumer.EditProfileUser"
            android:exported="false" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".consumer.MainPageActivity"
            android:exported="false" />
        <activity
            android:name=".ChooseTypeActivity"
            android:exported="false" />
        <activity
            android:name=".RegisterRestActivity"
            android:exported="false" />
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>